[package]
name = "libykpers-sys"
version = "0.1.0"
authors = ["Vladimir Lushnikov <vladimir@solidninja.is>"]
description = "Rust bindings to libykpers"
links = "ykpers-1"
build = "build.rs"

[lib]
name = "libykpers_sys"

[dependencies]
libc = "0.2"

[build-dependencies]
pkg-config = "0.3"
