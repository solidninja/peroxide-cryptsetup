[package]
name = "libykpers-sys"
version = "0.1.0"
authors = ["Vladimir Lushnikov <vladimir@solidninja.is>"]
description = "Rust bindings to libykpers"
links = "ykpers-1"
build = "build.rs"

[lib]
name = "libykpers_sys"

#FIXME remove this
[[bin]]
name = "tester"

[dependencies]
libc = "*"

[build-dependencies]
pkg-config = "*"
