language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly 
sudo: required
dist: trusty
os:
  - linux
install:
  - sudo apt-get update
  - sudo apt-get install libcryptsetup-dev
  - which cargo
script:
  - cargo build --verbose
  - sudo -E /usr/local/bin/cargo test --verbose
# addons:
#   apt:
#     packages:
#       - libcryptsetup-dev
